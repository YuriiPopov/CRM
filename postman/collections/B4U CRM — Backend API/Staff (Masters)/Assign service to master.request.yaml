$kind: http-request
url: "{{baseUrl}}/staff/{{masterId}}/services"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "serviceId": "{{serviceId}}"
    }
order: 6000
