$kind: http-request
url: "{{baseUrl}}/clients/{{clientId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "notes": "Обновлённая заметка",
      "tags": ["vip", "постоянный"]
    }
order: 4000
